package io.landysh.inflor.java.core;

public interface SingleCellTable {
	
	//// Data groking/retrieval
	// return String[] 	 frame UUIDs
	// get canonical parameter names (UUID)
	// get display parameter names (UUID)
	// get common canonical parameter names 
	// get common display parameter names 
	// return double[][] frame data (UUID)
	// return double[][] frame preview (UUID)
	// return double[][] preview (downsample type, sample value (0-1 fraction, >1 floor count))
	// return double[][] all data in memory?
	
	//// Compensation
	//// Transformation
	//// Gating 
}
